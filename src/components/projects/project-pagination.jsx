"use client";

import React from "react";
import Pagination from "@/components/shared/pagination";

export default function ProjectPagination(props) {
  return <Pagination itemName="projects" {...props} />;
}
